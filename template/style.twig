#pwcard td, #pwcard th {
    border: 1px solid #273D4C;
    font-size: 1.2em;
    padding: .25rem !important;
}

#pwcard td:nth-of-type({{ env('MATRIXSTROKE')|default(3) }}n+{{ env('MATRIXSTROKE')|default(3) + 1 }}), #pwcard th:nth-of-type({{ env('MATRIXSTROKE')|default(3) }}n+{{ env('MATRIXSTROKE')|default(3) + 1 }}), #pwcard td:nth-last-child(2), #pwcard th:nth-last-child(2), #pwcard td:first-of-type, #pwcard th:first-of-type {
    border-right: 4px solid #273D4C;
}

#pwcard td:last-of-type, #pwcard th:last-of-type {
    border-right: 1px solid #273D4C;
}

#pwcard tbody tr:nth-of-type({{ env('MATRIXSTROKE')|default(3) }}n) td, #pwcard th {
    border-bottom: 4px solid #273D4C;
}

#pwcard tbody tr:last-of-type td {
    border-bottom: 4px solid #273D4C;
}

#pwcard td, #pwcard th {
    text-align: center;
}

#pwcard th, #pwcard tfoot, #pwcard .mainpres {
    font-weight: bold;
    color: #fff;
    background: #927A19;
}

#pwcard .plain {
    background: #273D4C;
}

#pwcard>tbody>tr:nth-of-type(even) {
    background: #E9E1CA;
}

summary {
    cursor: pointer;
}

summary:focus {
    outline: none;
}

.logo {
    width: 100%;
    height: 100px;
}

.jumbotron {
    background: #44697D;
    color: white;
    font-family: Univers, Arial;
    padding: 10px;
}

h1 {
    font-size: 250%;
}

p {
    font-size: 90%;
}

img {
    max-width: 200px;
    max-height: 100px;
    float: right;
}

.btn {
    margin-top: 10px;
    background: #44697D;
    border: #44697D;
    float: right;
}

.btn:hover {
    background: #9A8419;
}

.printOnly {
    display: none; 
}

@media print {
    .printOnly {
        display: block;
    }
    * {
        font-family: Univers, Arial;
    }
    h1 {
        font-size: 250%;
        color: black !important;
    }
    p {
        display: none;
    }
    .btn {
        display: none;
    }
    details {
        display: none;
    }

    #pwcard td, #pwcard th {
     -webkit-print-color-adjust:exact;
    border: 1px solid #273D4C !important;
    font-size: 1.2em !important;
    padding: .25rem !important;
    }

    #pwcard td:nth-of-type({{ env('MATRIXSTROKE')|default(3) }}n+{{ env('MATRIXSTROKE')|default(3) + 1 }}), #pwcard th:nth-of-type({{ env('MATRIXSTROKE')|default(3) }}n+{{ env('MATRIXSTROKE')|default(3) + 1 }}), #pwcard td:nth-last-child(2), #pwcard th:nth-last-child(2), #pwcard td:first-of-type, #pwcard th:first-of-type {
        border-right: 4px solid #273D4C !important;
        -webkit-print-color-adjust:exact;
    }

    #pwcard td:last-of-type, #pwcard th:last-of-type {
        border-right: 1px solid #273D4C !important;
         -webkit-print-color-adjust:exact;
    }

    #pwcard tbody tr:nth-of-type({{ env('MATRIXSTROKE')|default(3) }}n) td, #pwcard th {
        border-bottom: 4px solid #273D4C !important;
         -webkit-print-color-adjust:exact;
    }

    #pwcard tbody tr:last-of-type td {
        border-bottom: 4px solid #273D4C !important;
         -webkit-print-color-adjust:exact;
    }

    #pwcard td, #pwcard th {
        text-align: center !important;
         -webkit-print-color-adjust:exact;
    }

    #pwcard th, #pwcard tfoot, #pwcard .mainpres {
        font-weight: bold !important;
        color: #fff !important;
        background: #927A19 !important;
         -webkit-print-color-adjust:exact;
    }

    #pwcard .plain {
        -webkit-print-color-adjust:exact;
        background: #273D4C !important;
    }

    #pwcard>tbody>tr:nth-of-type(even) {
        -webkit-print-color-adjust:exact;
        background: #E9E1CA !important;
    }
}
